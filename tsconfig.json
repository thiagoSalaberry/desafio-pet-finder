{
    "compilerOptions": {
        "target": "ESNext",
        "module": "CommonJS",
        "moduleResolution": "node",
        "resolveJsonModule": true,
        //Al correr el comando tsc lo va a hacer a la carpeta de outDir, todo lo que esté en includes
        "outDir": "dist"
    },
    "includes": ["./be-src/**/*.ts"],
    "exclude": ["./fe-src"]
}